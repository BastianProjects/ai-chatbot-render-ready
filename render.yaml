
services:
  - type: web
    name: ai-chatbot
    env: python
    buildCommand: ./build.sh && pip install -r requirements.txt
    startCommand: streamlit run PDFEnhancedChatBot_WithVideoSupport.py
    plan: free
